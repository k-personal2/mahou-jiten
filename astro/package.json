{
  "name": "rpg-magic-dictionary",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "node scripts/fix-routes.mjs",
    "dev": "astro dev",
    "build": "node scripts/validate-data.mjs && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "validate": "node scripts/validate-data.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.6.0"
  }
}
